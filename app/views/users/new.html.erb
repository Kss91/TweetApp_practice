<%= stylesheet_link_tag "posts", "data-turbo-track": "reload" %>
<div class="container">
  <h1>新規登録画面</h1>
  <p>ここでは新規登録（User）の内容を記載</p>
    <% @user.errors.full_messages.each do |message| %>
      <div class="error-message">
        <%= message %>
      </div>
    <% end %>
  <%= form_tag("/users/create", {multiple: true}) do %>
    <p>ユーザー名</p>
    <input class="form" name="name">
    <p>メールアドレス</p>
    <input class="form" name="email">
    <p>プロフィール画像</p>
    <input class="form" type="file" name="image_name">
    <input type="submit" value="新規登録">
  <% end %>
</div>
